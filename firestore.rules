rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Family app — open read/write. Tighten if needed.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
